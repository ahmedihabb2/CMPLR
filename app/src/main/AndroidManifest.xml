<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cmp.cmplr">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CMPLR">
        <activity
            android:name=".MainActivity"
            android:exported="true" />
        <activity
            android:exported="true"
            android:name=".View.Activities.SplashActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.Activities.LoginActivity"></activity>
        <activity android:name=".View.Activities.SignupActivity" />
        <activity android:name=".View.Activities.WritePostActivity"
            android:windowSoftInputMode="stateVisible|adjustNothing">
        </activity>
        <activity android:name=".View.Activities.ForgotActivity"></activity>
        <activity android:name=".View.Activities.MainScreenActivity"></activity>
        <activity android:name=".View.Activities.AgeActivity"></activity>
        <activity
            android:name=".View.Activities.IntroActivity"
            android:exported="true"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>